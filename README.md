# 🏦 Banking Activity Simulator (Java + Maven)

A simple banking system simulator built using **Core Java** and **Maven**, with **JDBC** for database interaction (MySQL/MariaDB).

## 📁 Project Structure
- `model/` — POJO classes for entities like Account, Customer, Transaction
- `dao/` — Database access layer using JDBC
- `service/` — Business logic layer (deposit, withdraw, transfer)

## 🧰 Technologies Used
- Java (JDK 17+)
- Maven for dependency management
- JDBC for DB connectivity
- MySQL / MariaDB
- Junit testing

## 🪶 Features
- Create and manage accounts
- Deposit and withdraw funds
- Transfer between accounts
- View transaction history
- Simple text-based or servlet-driven interface

